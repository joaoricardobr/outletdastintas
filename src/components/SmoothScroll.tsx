"use client";
// SmoothScroll — uses native CSS scroll-behavior, no external libs needed
// The html { scroll-behavior: smooth } in globals.css handles this
export default function SmoothScroll() {
  return null;
}
